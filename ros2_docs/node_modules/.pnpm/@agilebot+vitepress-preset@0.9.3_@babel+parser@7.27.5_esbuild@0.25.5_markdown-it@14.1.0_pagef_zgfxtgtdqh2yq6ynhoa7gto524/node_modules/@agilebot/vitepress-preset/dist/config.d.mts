import { Locale, Locale as Locale$1 } from "locale-enum";
import { PagefindOption, SearchConfig } from "vitepress-plugin-pagefind";
import { DefaultTheme, LocaleSpecificConfig, UserConfig } from "vitepress";
import { VitePressSidebarOptions } from "vitepress-sidebar/types";

//#region src/plugins/knowledge/types.d.ts
interface KnowledgeOptions {
  /** When passed, show the "Ask AI" button and chat window. */
  serverUrl?: string;
  /** Defaults to "knowledge", and your files will be available at `https://your-domain/knowledge/*` */
  dir?: string;
  /**
   * Break up your paths into multiple knowledge files. This object is a map of base paths to output filenames.
   * @default { "/": "docs" }
   */
  paths?: Record<string, string>;
  /**
   * Markdown files to ignore. Note that the 404.md page is always ignored.
   */
  ignore?: string[];
  /** Default selector to only process content from inside. */
  selector?: string;
  /** Customize the selector used for each page layout. */
  layoutSelectors?: Record<string, string>;
  /** Customize the selector for individual pages. */
  pageSelectors?: Record<string, string>;
}
//#endregion
//#region src/config/types.d.ts
type ViteConfig = Extract<UserConfig['vite'], object>;
type LocaleConfig<ThemeConfig> = Partial<Record<Locale$1, LocaleSpecificConfig<ThemeConfig> & {
  label: string;
  lang: string;
  link?: string;
}>>;
type AgilebotUserConfig<ThemeConfig = any> = Omit<UserConfig<ThemeConfig>, 'lang' | 'locales' | 'vite'> & {
  /**
   * Document Root
   * @example /path/to/docs
   */
  docRoot: string;
  /**
   * VitePress Root
   * @example /path/to/docs/.vitepress
   */
  vpRoot: string;
  /**
   * Locales
   */
  locales?: LocaleConfig<ThemeConfig>;
  /**
   * Plugin Config
   */
  pluginConfig?: {
    /**
     * Options for `vitepress-sidebar`
     * @see https://www.npmjs.com/package/vitepress-sidebar
     */
    sidebar?: VitePressSidebarOptions;
    /**
     * Options for `vitepress-plugin-pagefind`
     * @see https://www.npmjs.com/package/vitepress-plugin-pagefind
     */
    pagefind?: SearchConfig & PagefindOption;
    /**
     * Options for `knowledge`
     */
    knowledge?: KnowledgeOptions;
  };
  /**
   * Vite Config
   */
  vite?: ViteConfig;
};
//#endregion
//#region src/config/factory.d.ts
declare function withAgilebotConfig<ThemeConfig = DefaultTheme.Config>(config: AgilebotUserConfig): Promise<UserConfig<NoInfer<ThemeConfig>>>;
//#endregion
export { type AgilebotUserConfig, Locale, withAgilebotConfig };